# Deploy updated portfolio

Copy these files over your local `geoai-portfolio` clone, then:

```bash
git add .
git commit -m "feat: expand GeoAI portfolio projects and repositories"
git push origin main
```

GitHub Pages will rebuild from `main`.
